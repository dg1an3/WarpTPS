name: C/C++ CI (Legacy)

# This workflow is deprecated. Please see cmake.yml for the current CMake-based build.
# Kept for historical reference only.

on:
  workflow_dispatch:  # Manual trigger only

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Notice
      run: |
        echo "This workflow is deprecated."
        echo "Please use the cmake.yml workflow instead."
        echo "The project now uses CMake with Visual Studio 2022 on Windows."
